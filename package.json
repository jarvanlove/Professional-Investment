{
  "name": "professional-investment",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web \"uv run --directory services/quant-api uvicorn app.main:app --reload --port 8010\" \"pnpm --dir apps/web dev -- -p 3010\"",
    "dev:api": "uv run --directory services/quant-api uvicorn app.main:app --reload --port 8010",
    "dev:web": "pnpm --dir apps/web dev -- -p 3010",
    "test": "uv run --directory packages/quant-core pytest && uv run --directory services/quant-api pytest && pnpm --dir apps/web test"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
